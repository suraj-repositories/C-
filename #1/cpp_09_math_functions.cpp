#include <iostream>
#include <cmath>

int main(){

    // Reference docs : https://cplusplus.com/reference/cmath/

    double x = 3; 
    double y = 4; 
    double z; 

    // z = std::max(x, y);
    // z = std::min(x, y);
    // z = pow(2, 3);
    // z = sqrt(9);
    // z = abs(-3);  // convert -ve to +ve
    // z = round(x);
    // z = ceil(x);
    // z = flour(x);

    std::cout << z;
    return 0;
}
